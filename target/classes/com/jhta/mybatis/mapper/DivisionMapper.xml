<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.mapper.division">
	<select id="getinfo" parameterType="int" resultType="division">
		select * from division where divisionNum=#{divisionNum} 
	</select>
	<select id="slist" parameterType="int" resultType="division">
		select * from division where divisionNum=#{divisionNum} 
	</select>
</mapper>